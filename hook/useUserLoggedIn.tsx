// Codes by mahdi tasha
// Creating a custom hook which checks if user is logged in and exporting it as default
 export default function useUserLoggedIn():boolean {
    // TODO: This will change when working with backend
    return true;
}