// Codes by mahdi tasha
// Importing type of nextJS configs
/** @type {import('next').NextConfig} */

// Defining configs of nextJS
const nextConfig = {};

// Exporting the configs
export default nextConfig;